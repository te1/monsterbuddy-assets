{
  "name": "monsterbuddy-assets",
  "private": true,
  "scripts": {
    "icons": "squoosh-cli --oxipng auto icons --output-dir icons",
    "render": "squoosh-cli --oxipng auto render --output-dir render",
    "monsters:crop": "node scripts/crop.js",
    "eggs": "svgo -f eggs -o eggs",
    "types": "svgo -f types -o types",
    "weapons": "svgo -f weapons -o weapons"
  },
  "devDependencies": {
    "@squoosh/cli": "^0.7.1",
    "sharp": "^0.28.3",
    "svgo": "^2.3.1"
  }
}
